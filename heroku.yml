build:
  docker:
    web: Dockerfile
run:
  web: gunicorn gallery_api.wsgi:application --bind :8000
release:
  image: web
  command:
    - python manage.py collectstatic --noinput
    - python manage.py migrate
